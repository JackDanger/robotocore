[package]
name = "robotocore-rust"
version = "0.1.0"
edition = "2021"
description = "Rust port of robotocore S3 routing logic"

[dependencies]
regex = "1.10"
once_cell = "1.19"
parking_lot = "0.12"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]